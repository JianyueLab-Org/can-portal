# 本地开发。三个都不是密钥 —— 它们是主机名，容器里也是明文环境变量。
#
# 这个站**一个 Secret 都没有**，和 can-efb / can-controller 一样。会话由 can-api
# 签在父域上，这里只负责转发 cookie。哪天有人要在这里加一个 Secret，先确认那件
# 事不能靠转发 cookie 完成。
#
# PUBLIC_ORIGIN 本地一定要设：写操作会拿浏览器发来的 Origin 和它比对，不设就是
# 拿线上地址去比对 http://localhost:4328，于是晋升、活动、奖品、公示的每一次保存
# 在本地永远 403。

# can-api。数据全部来自它；不指过来就是读线上的真实数据 —— 而这个站的写操作会
# 真的晋升一个人、真的结算一场活动的积分。本地开发前先想清楚这一条。
CAN_API_ORIGIN=https://api.ceruleanavi.net

# can-web。登录页，以及侧栏里那一排还留在主站的跨站链接。
CAN_WEB_ORIGIN=https://ceruleanavi.net

# 本站自己的对外地址。
PUBLIC_ORIGIN=http://localhost:4328

# SweatBox 参考数据的来源。只有跑 `bun run build:sweatbox` 时才要，日常开发不需
# 要 —— `src/data/sweatbox/` 已经提交在仓库里了。
# SECTOR_REPO=~/Documents/Dev/CeruleanAviationNetwork/Sector
