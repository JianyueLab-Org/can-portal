{
  // `astro check` reports 0 errors for a Vue SFC no matter what is in it — it
  // only diagnoses .astro/.ts. This site's whole interactive half is Vue (the
  // shell, the dashboard, the ATIS maker, the reservation board), so without
  // this run there is effectively no type gate on it at all. vue-tsc covers
  // that half; .astro files are excluded here because vue-tsc cannot parse
  // them, so the two runs are complementary and `bun run typecheck` invokes
  // both.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": [".astro/types.d.ts", "src/**/*.ts", "src/**/*.vue"],
  "exclude": ["node_modules", "dist"]
}
